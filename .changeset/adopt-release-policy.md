---
"@ait-co/devtools": minor
---

Adopt the organization-wide release policy: switch from release-please to
Changesets, and bump to `0.1.0` as the pre-stable baseline. All packages in
`apps-in-toss-community` will stay on `0.1.x` (patch-only) until the first
official `1.0.0` release coordinated with `agent-plugin`.
